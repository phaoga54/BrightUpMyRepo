export type LiveNowType = {
    avatar: string
    name: string
    views: number
    title: string
    imageUrl: string
}

export type TrendingType = {
    avatar: string
    name: string
    views: number
    title: string
    imageUrl: string
}